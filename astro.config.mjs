// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// Update to https://feliciali.com once DNS is moved (Phase 3) — until then
	// GitHub Pages serves from xinyufeliciali.github.io.
	site: 'https://xinyufeliciali.github.io',
});
